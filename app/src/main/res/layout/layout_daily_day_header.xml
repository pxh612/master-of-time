<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="ui"
            type="com.example.master_of_time.screens.dailyday.event.screen.DdEventFragment" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">


        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            android:layout_marginStart="@dimen/marginRight_add_adapterDailyDayHeader"

            android:text="GROUPS"
            android:onClick="@{(view) -> ui.onClick(view)}"
            android:id="@+id/buttonOne" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toLeftOf="@id/add"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="@dimen/marginRight_add_adapterDailyDayHeader"

            tools:text="1"
            android:text="layout"
            android:onClick="@{(view) -> ui.onClick(view)}"
            android:id="@+id/layout" />

        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="@dimen/marginRight_add_adapterDailyDayHeader"

            android:text="@string/text_add_fragmentDailyDay"
            android:onClick="@{(view) -> ui.onClick(view)}"
            android:id="@+id/add" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
